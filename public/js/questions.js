//eslint-disable-next-line no-empty-function
/*$(document).ready(function() {

    var question = [""];
    var answer = 

    app.get("/:id", function(req, res) {
        connection.query("SELECT * FROM Questions where id = ? ORDER BY RAND() LIMIT 5", [req.params.id], function(err, data) { 
            if (err) {
                return res.status(500).end();
            }
        }
    });




});*/
